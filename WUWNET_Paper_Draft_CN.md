# 低功耗水声扩频通信的系统级时空联合均衡架构

## 1. 摘要 (Abstract)

在极低信噪比（SNR）的深海环境中，水声直接扩频（UW-DSSS）通信受限于多径干扰引起的频率选择性衰落以及非平稳多普勒频移。为保证自主水下航行器（AUV）与水面浮标节点的追踪稳定性，本文提出了一种稳健的系统级时空联合均衡架构。在空间-时延域，引入混合阈值时间反转镜（TRM）预聚焦机制。该机制将有序统计恒虚警率（OS-CFAR）与确定性旁瓣底限相约束，在精准提取稀疏多径组件的同时，严格抑制由旁瓣引起的自相关干扰。在时间-多普勒域，构建异方差变分贝叶斯自适应卡尔曼滤波（HVB-AKF）算法。通过提取差分软符号包络作为物理条件异方差惩罚因子，在变分贝叶斯迭代中动态缩放量测噪声协方差，迫使滤波器在深度衰落期间进入“惯性滑行”状态，从而彻底切断判决反馈中的误差传播。此外，为克服静态 TRM 模板固有的“信道老化”物理退化，设计信噪比感知的动态路由方案，在高信噪比条件下自适应旁路 TRM 模块。基于 Bellhop 射线追踪模型的物理信道仿真表明，该架构在 -10 dB 信噪比下展现出极强的同步鲁棒性，在动态物理声场中较传统启发式基准获得高达 4.5 dB 的系统级增益。

## 2. 引言 (Introduction)

水声直接扩频（UW-DSSS）通信的物理层同步极易受复杂海洋动力学环境破坏。对于自主水下航行器（AUV）与水面浮标而言，实现稳健同步面临两大物理挑战：长时延多径扩展（ISI）引起的频率选择性衰落，以及由平台相对运动与瞬态海面波浪引发的非平稳多普勒频移。在极低信噪比（SNR $\le -10\text{ dB}$）工况下，传统非相干延迟锁定环（DLL）常发生严重动态滞后与追踪发散。

为应对高动态多普勒追踪挑战，前期工作提出了基于结构正则化的创新自适应卡尔曼滤波（IAE-AKF）[1]。然而，原始 IAE-AKF 架构在真实海洋环境中显露局限。首先，IAE-AKF 依赖滑动窗口等启发式规则估计协方差，在信号陷入深衰落时响应迟缓，引发判决反馈追踪的误差雪崩。其次，该模型聚焦于多普勒频移，忽略了浅水或复杂地形下长时延多径扩展造成的严重码间干扰（ISI）。

水下信号处理领域的最新研究引入变分贝叶斯（VB）推断以应对非平稳噪声环境 [2]，并利用时间反转镜（TRM）技术实现多径能量聚焦 [3]。然而，标准 VB 框架虽擅长捕获非平稳噪声，但其迭代收敛在信号深度衰落瞬间剧烈放缓，导致判决导向追踪极易发生误差雪崩。另一方面，基于数据包前导码估计的静态 TRM 模板在长数据包持续期间面临严峻的“信道老化（Channel Aging）”现象，由此产生的信道失配会在高信噪比条件下产生人为误差底板。

针对上述挑战，本文提出一种面向严苛水声网络的高鲁棒性系统级时空均衡架构。主要贡献如下：
1. 构建**异方差变分贝叶斯自适应卡尔曼滤波 (HVB-AKF)** 以根除误差传播。利用差分软符号包络作为瞬时可靠性度量，将条件异方差惩罚因子嵌入变分 M 步，迫使滤波器在深衰落时进入“惯性滑行”状态，实现最大鲁棒性。
2. 引入**混合阈值 TRM 预聚焦**机制。通过确定性 LFM 旁瓣底限约束统计学 OS-CFAR，确保稀疏多径组件的精准提取，从根本上防止低噪声域内旁瓣引起的自相关干扰。
3. 设计系统级**信噪比感知动态路由**架构。依据前导码 SNR 预估自适应旁路 TRM 模块，解决空间聚焦增益与时间信道老化畸变之间的物理冲突，消除高 SNR 误差底板。
4. 基于高保真 Bellhop 射线追踪模型进行架构验证。跨越多种水下地形剖面的仿真证实了该架构的鲁棒性、物理一致性及其相对于启发式方法的显著追踪增益。

## 3. 相关工作 (Related Work)

水下声学通信本质上受限于恶劣信道环境与严苛节点资源。本节围绕鲁棒多普勒追踪、TRM 多径抑制及物理声场建模三个维度评述现有文献。

### 3.1 鲁棒多普勒追踪与贝叶斯推断
时变水声信道中的相干与半相干接收机高度依赖精确的载波相位与多普勒追踪。自适应卡尔曼滤波（AKF），如前期提出的 IAE-AKF [1]，已被用于缓解噪声基底波动带来的性能退化。

为系统处理海洋环境非平稳噪声，近期文献转向高级概率推断框架。例如，文献 [2] 提出变分贝叶斯扩展卡尔曼滤波（VB-EKF），通过变分迭代自适应估计量测与过程噪声协方差。尽管 VB 方法在追踪缓慢变化的非平稳噪声方面表现优异，但其 M 步协方差估计在面对快速、深度的信号衰落时极其脆弱。在深衰落期间，可靠信号能量缺失导致概率推断收敛不良，使得判决导向追踪易受灾难性误差传播的影响。为突破 VB 方法在极端水声衰落下的根本局限，本文提出的 HVB-AKF 架构在标准 VB 迭代中注入基于差分软符号包络的瞬时物理可靠性指标，赋予系统即刻滑行的防御能力。

### 3.2 时间反转镜多径抑制与信道老化挑战
时间反转镜（TRM）凭借时空匹配滤波特性，在水下多径均衡与 ISI 抑制中展现出卓越效能。当前，TRM 广泛充当预聚焦前端 [4]。

然而，传统 TRM 研究多假设长包传输期内信道平稳或依赖 CIR 的连续更新。在真实动态海洋环境中，源自前导码的静态 TRM 模板必然遭遇**信道老化**。随数据包传输，瞬态海浪与节点漂移致使实际 CIR 与前导码 CIR 去相关。在高信噪比（SNR）工况下，环境噪声极低，主导误差源转变为接收信号与陈旧 TRM 模板卷积引发的失配畸变。该失配导致误差底板，阻碍传统 TRM 系统在优质信道中逼近理论性能。此外，传统 TRM 多径提取依赖统计恒虚警率（CFAR），无视 LFM 匹配滤波固有的约 -13 dB 确定性自相关旁瓣。在低噪声域，纯 CFAR 会将旁瓣误判为真实多径，引入自相关干扰。本文提出的混合阈值 TRM 与 SNR 感知动态路由架构旨在系统性解决上述旁瓣干扰与信道老化问题。

## 4. 系统模型与高逼真水声信道仿真 (System Model)

为严谨形式化微型水下节点的通信过程，本节确立 DQPSK/DSSS 信号的基带表示、非平稳多普勒运动学状态空间模型及基于 Bellhop 的射线追踪物理信道仿真。

### 4.1 水声 DSSS 信号与非平稳多普勒模型
考虑基于差分四相相移键控（DQPSK）的水声直接扩频（UW-DSSS）系统。设 $b_k \in \{ \pm 1 \pm j \}$ 为第 $k$ 个原始数据符号。为克服水下传播固有的相位模糊，发射端进行差分编码：$d_k = d_{k-1} \cdot b_k$。差分符号 $d_k$ 经伪随机（PN）序列 $c(t)$ 扩频，生成基带发射信号：
$$ s_{bb}(t) = \sum_{k} d_k \cdot c(t - k T_s) $$
其中 $T_s$ 为扩频符号周期。

水下传播过程中，接收信号受节点漂移与海浪起伏引起的时变多普勒频移 $f_d(t)$ 与载波相位漂移 $\theta(t)$ 调制。经重采样粗补偿后，残余频移与相位通过离散二维运动学状态空间模型追踪：
$$ \mathbf{x}_k = \begin{bmatrix} \theta_k \\ f_{d,k} \end{bmatrix} = \begin{bmatrix} 1 & 2\pi T_s \\ 0 & 1 \end{bmatrix} \begin{bmatrix} \theta_{k-1} \\ f_{d,k-1} \end{bmatrix} + \mathbf{w}_k $$
其中 $\mathbf{x}_k$ 为相位与频移状态向量，$\mathbf{w}_k \sim \mathcal{N}(0, \mathbf{Q}_k)$ 表示湍流与加速度引起的过程噪声。

在解扩与判决反馈环路中，由差分软符号导出的相位量测方程为：
$$ z_k = \text{angle}(s_k s_{k-1}^*) = \mathbf{H} \mathbf{x}_k + v_k $$
其中量测矩阵 $\mathbf{H} = [1, 0]$，瞬时量测噪声 $v_k \sim \mathcal{N}(0, R_k)$。

### 4.2 Bellhop 射线追踪物理信道仿真
传统统计信道假设脱离真实声场的空间几何约束。为构建物理严谨的评估环境，本文采用 Bellhop 射线追踪模型仿真高保真信道。

在二维柱坐标系 $(r, z)$ 下，高频声压场 $P(r, z)$ 满足 Helmholtz 方程的射线近似，声线轨迹遵循程函方程（Eikonal equation）。Bellhop 根据海底几何形貌与声速剖面（SSP）求解本征声线，计算时延 $\tau_p$、幅度衰减 $A_p$ 及边界反射相移。时变声学信道冲激响应（CIR）表示为：
$$ h(t, \tau) = \sum_{p=1}^{P} A_p(t) \cdot e^{j \phi_p(t)} \cdot \delta\big(\tau - \tau_p(t)\big) $$
其中 $P$ 为有效本征声线数，$A_p(t)$ 计入几何扩展与由 Thorpe 公式计算的海水吸收损耗。

本研究设定两种真实海洋场景：
1. **深海剖面**：水深 4000 m，距离 45 km，具备典型 Munk SSP。声线在 SOFAR 声道折射，接收信号经历数十毫秒长时延多径扩展与极低信噪比衰减（$\le -10\text{ dB}$）。
2. **浅海剖面**：水深 100 m，距离 20 km，具备表面声道与频繁海面/海底反射。高阶反射引发密集强多径扩展（严重 ISI），海浪激起非平稳多普勒波动。

物理信道输出的基带接收信号为：
$$ r(t) = s_{bb}(t) \otimes h(t, \tau) e^{j (2\pi f_{d} t + \theta_0)} + n(t) $$
其中 $n(t)$ 为海洋环境白噪声。

## 5. 提出的系统级时空联合架构 (Proposed Architecture)

为解决频率选择性衰落与非平稳多普勒的双重挑战，本节提出计算严谨的系统级级联时空架构。

### 5.1 混合阈值 TRM 预聚焦
在复杂浅水环境中，初始 CIR 估计 $\hat{h}(t)$ 由接收信号与 LFM 前导码的互相关获取：
$$ \hat{h}(t) = \int r(\tau) s_{ref}^*(\tau - t) d\tau $$

传统系统依赖 CFAR 算法剥离混响噪声。然而，由于 LFM 匹配滤波存在固有约 -13 dB 的确定性自相关旁瓣，纯统计 OS-CFAR 在极低底噪下会跌破旁瓣电平，导致错误提取旁瓣并引入自生干扰。

为此，提出**混合阈值**提取机制，定义动态截断阈值 $\gamma_{hybrid}$ 为统计 OS-CFAR 阈值 $\gamma_{cfar}$ 与确定性旁瓣底限 $P_{sidelobe}$ 之间的最大值：
$$ \gamma_{hybrid} = \max(\gamma_{cfar}, P_{sidelobe}) $$
信噪比提升后的等效 CIR 为：
$$ \hat{h}_{ext}(t) = \begin{cases} \hat{h}(t), & |\hat{h}(t)| > \gamma_{hybrid} \\ 0, & \text{否则} \end{cases} $$
经时间反转卷积 $y(t) = r(t) \otimes \hat{h}_{ext}^*(-t)$，将多径能量相干压缩，为后续追踪环路提供高信噪比输入。

### 5.2 异方差变分贝叶斯追踪 (Heteroscedastic Variational Bayesian Tracking)
空间聚焦后，等效基带信号仍受时变多普勒频移干扰。在判决导向环路中，信号深衰落会触发灾难性失效：硬判决错误致使新息蜕变为纯噪声，标准自适应滤波器盲目吸收该误差，导致追踪即刻发散。

尽管变分贝叶斯（VB）框架可通过变分参数 $\alpha_R, \beta_R$ 更新逆伽马先验，自适应估计非平稳量测噪声协方差 $R_k$，但标准 VB 更新对瞬时信号丢失极其敏感。为此，我们构建**异方差变分贝叶斯自适应卡尔曼滤波（HVB-AKF）**。其物理核心在于提取差分软符号输出包络 $m_k = |s_k s_{k-1}^*|$ 作为判决可靠性的瞬时物理度量。

设计基于软包络驱动的条件异方差惩罚因子 $\Lambda(m_k)$：
$$ \Lambda(m_k) = \frac{c_1}{m_k^2 + c_2} $$
其中 $c_2$ 控制绝对信号丢失时的惩罚上限，$c_1 = 1 + c_2$ 保证在可靠接收（$m_k \to 1$）时惩罚因子归一化为 1。依据深衰落边界的系统标定，设定 $c_2 = 1/50$，允许最大惩罚放大系数为 51。

在 VB 迭代的 M 步中，基准量测噪声估计 $R_{vb} = \beta_R / \alpha_R$ 经瞬时异方差惩罚因子的动态缩放，生成最终自适应协方差：
$$ R_k^{adaptive} = \frac{\beta_R}{\alpha_R} \cdot \Lambda(m_k) $$

当信号陷入深衰落（$m_k \to 0$）时，惩罚因子剧增至上限。人为膨胀的 $R_k^{adaptive}$ 迫使卡尔曼增益 $K_k \to 0$。状态更新逻辑 $\hat{\mathbf{X}}_{k|k} \approx \hat{\mathbf{X}}_{k|k-1}$ 使环路自然进入“惯性滑行（Inertial Coasting）”状态，从根本上阻断误差传播。信道恢复时（$m_k \approx 1$），$\Lambda(m_k) \to 1$，标准 VB 追踪平滑恢复。此架构兼具 VB 的长效噪声底追踪能力与软判决惩罚的瞬时防御能力。

### 5.3 信噪比感知的动态路由架构
尽管 TRM 在低 SNR 环境下聚焦增益显著，但在长数据包传输中暴露出**信道老化**这一物理矛盾。高 SNR 条件（如 $>-9\text{ dB}$）下，信道底噪微弱，主导误差源转为接收信号与陈旧 TRM 模板卷积引发的畸变，产生人为误差底板。

为化解此冲突，引入**信噪比感知动态路由**架构。在前导码同步阶段，估算瞬时前导码 SNR，记为 $\text{SNR}_{est}$：
$$ \text{路由决策} = \begin{cases} \text{旁路 TRM}, & \text{若 } \text{SNR}_{est} > \text{SNR}_{th} \\ \text{激活 TRM}, & \text{若 } \text{SNR}_{est} \le \text{SNR}_{th} \end{cases} $$
结合本文水下声学配置，旁路阈值设定为 $\text{SNR}_{th} = -9\text{ dB}$。该自适应旁路逻辑确保节点在低 SNR/强多径下榨取 TRM 空间增益，而在原始信号质量充足时果断旁路，消除信道老化引起的误码底板。

## 6. 仿真结果与物理场分析 (Performance Evaluation)
依靠 MATLAB 结合 Bellhop 物理信道模型展开严谨验证。

### 6.1 空间聚焦与多径抑制
在浅水物理剖面中，原始 CIR 存在严重长延时多径扩展。本文的混合阈值 TRM 机制 $\max(\gamma_{cfar}, P_{sidelobe})$ 严格滤除 LFM 自相关旁瓣假象，成功将弥散多径能量相干压缩为尖锐主峰，为时域追踪提供结构清晰的信号输入。

### 6.2 追踪稳定性与防发散机制
在 SNR = -8 dB 的恶劣工况下，基准启发式算法（IAE-AKF）因吸收不可靠相位量测产生剧烈发散。相反，HVB-AKF 将误差轨迹严格约束在收敛界内。物理上，随着瞬时软符号包络 $m_k$ 跌入噪声底，异方差惩罚因子迫使卡尔曼增益归零，以惯性滑行策略拒绝破坏性反馈，确保极恶劣声学环境下的同步存活。

### 6.3 系统级 BER 性能与路由逻辑验证
隔离测试证明：纯 AKF 在未解析的 ISI 面前全面崩溃；启发式基准仅在 0 dB SNR 下勉强达到 $10^{-3}$ 误码率（BER）。本文联合架构在约 -4.5 dB SNR 即可实现 $10^{-3}$ BER，较最佳基准提升 4.5 dB。此外，SNR 感知动态路由逻辑在 SNR $> -9\text{ dB}$ 时彻底消除静态 TRM 的误差底板，实现无差错传输。

### 6.4 复杂度与计算防御权衡 (Trade-off Defense)

| 算法名称 | 复杂度/符号 | ARM MCU 耗时 ($\mu s$) | Intel CPU 耗时 ($\mu s$) |
| :--- | :--- | :--- | :--- |
| 标准 EKF | $O(n_x^3)$ | 142.5 | 12.1 |
| 变分贝叶斯 VB-EKF | $O(N_{iter} n_x^3)$ | 685.0 | 54.3 |
| IAE-AKF (前期启发式) | $O(n_x)$ | 18.2 | 1.8 |
| **本文 HVB-AKF** | **$O(N_{iter} n_x^3)$** | **687.5** | **54.8** |

本文 HVB-AKF 算法引入的计算开销与标准变分贝叶斯方法相当（单符号耗时 687.5 $\mu s$）。然而，对于具备一定计算吞吐能力的 AUV 平台而言，这种算力投资在物理上是极其必要的。用计算开销换取深衰落期间强悍的抗雪崩稳定性，构成了深海极低信噪比通信中合理的系统级权衡设计。

### 6.5 物理场泛化鲁棒性 (Macroscopic Generalization)
为验证跨地形拓扑泛化能力，系统 BER 在深海 45 km、浅海 20 km 及起伏浅海斜坡三种差异悬殊的物理环境中展现出高度一致的收敛曲线。证明 TRM 空间聚焦与 HVB-AKF 时间追踪的严谨级联脱离了对局部水下地形的依赖，为未知海域部署提供宏观理论保证。

## 7. 结论 (Conclusion)
本文针对低功耗水声 DSSS 通信提出了稳健的系统级时空联合均衡架构。在空间域，混合阈值 TRM 在聚焦多径能量的同时严格防范确定性旁瓣干扰；在时间域，异方差变分贝叶斯自适应卡尔曼滤波（HVB-AKF）嵌入物理瞬时可靠性度量，动态惩罚量测噪声协方差，成功切断深衰落期间的追踪发散与误差雪崩。在系统路由层，SNR 感知动态路由架构克服 TRM 信道老化的物理矛盾，消除高 SNR 误差底板。基于 Bellhop 高保真水声物理信道的验证表明，所提架构具备卓越的鲁棒性，通过合理的计算权衡为 AUV 与高要求水下传感网络提供了高度实用的物理层同步解决方案。
