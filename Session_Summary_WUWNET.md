# WUWNET'26 项目会话记忆与核心技术总结 (Round 10.3 最终封版)

本文档用于记录当前工程的核心技术设定、审计与图表打磨进展，以便在后续的手稿撰写或未来的修订窗口中快速恢复完整的上下文记忆。

> [!NOTE]
> **会话最新里程碑 (2026-08-24)**：
> 1. **核心算法收敛**：完全确立了基于 **可靠性校准与固定状态噪声协方差矩阵 $Q$ 的扩展变分贝叶斯追踪机制 (E-FQ)**。
> 2. **3000-MC 出版级数据定稿**：彻底锁定了基于 Bellhop 三大剖面 (P1 浅海短基线, P2 浅海中基线, P3 深海长基线) 的 3000 次蒙特卡洛全量仿真结果，不再进行任何调参和重新仿真。
> 3. **图表与审计极严密闭环**：在 Round 10.1 - 10.3 中，完成了对 Pilot(200-MC) 缩放至 Paper(3000-MC) 级别的完美一致性验证 (72 行审计数据)，重建了带 95% 威尔逊置信区间的极清误差棒图表，并加入了对 Handle 句柄的绝对对象级审计，杜绝任何“数据造假”及“空图表”可能。

---

## 一、 项目背景与最终锁定的技术架构 (Round 10 范式)

**研究方向**：水声直接扩频通信 (UW-DSSS) 中的动态多径时延抗击与极低信噪比非平稳多普勒追踪。

目前系统锁定的核心对比基准与算法范式为：
- **基线算法 (IAE)**：理想自适应等效算法，传统固定平滑无动态变分能力的参考标准。
- **中间算法 (VB-FQ)**：标准变分贝叶斯-固定 $Q$ 架构，具备初步的方差自适应能力。
- **最终形态 (E-FQ)**：融合可靠性机制的固定 $Q$ 变分贝叶斯架构。在此架构中，$Q_{11}$ 和 $Q_{22}$ 严格分别固定为 $0.05$ 与 $0.002$，同时引入了动态包络可靠度因子 $m$。当信道发生深度衰落 (FADE) 时，自适应缩放观测噪声方差，有效避免了时延锁相环的崩溃。

---

## 二、 Round 10 大修与审计全纪录 (Critical Overhauls)

本次大版本迭代 (Round 8 - Round 10.3) 是一场针对“数据合法性”、“一致性”及“视觉展现力”的终极打磨：

1. **结构化数据的严密解耦**：
   - 弃用了杂乱的结构体透传，使用严格预定义的 `.csv` 宽表进行数据存储（包括 `final_ber_table.csv`, `final_tracking_table.csv`, `final_mechanism_table.csv` 等），确保数据的 R/Python 跨语言泛用性与永久审查可行性。
2. **Pilot $\rightarrow$ Paper 的完美一致性验证**：
   - 为确保在提交 3000-MC 正式耗时仿真前，小规模的 200-MC 先导实验得出的结论并非统计学偶然，我们实施了严格的**跨规模一致性审计**。
   - 验证成功捕捉到 2 处因样本量变大带来的 $1\text{ dB}$ 合法门限移动（P2 E-FQ SNR50 & P3 IAE SNR05）。
   - 4 大追踪效应与机制方向 100% 保持一致，稳固了核心科学宣称。
3. **彻底根除图表映射 Bug 与展现力重构**：
   - **Profile Key 强匹配**：通过 `canonical_profile_id.m` 的锚点级匹配，永久解决了类似 `P1` 被模糊匹配的灾难性 Bug，确保每一条线都精准溯源。
   - **高质量误差图表**：Fig1 (FER) 与 Fig2 (BER) 严格绘制了 95% Wilson C.I. 的误差棒，同时在对数坐标下对 $0$ 错误率进行了聪明的视觉地板映射，不污染原始 CSV 数据。
   - **多尺度面板解耦**：Fig4 机制分析图彻底重构为独立的三面板 ($m$, $R_{eff}/R_{vb}$, $K_{delay}$) 展示，并在图例中标注 $Q$ 的固定性，显著提升了图表的学术严谨性与美观度。
4. **强迫症级别的自我审查**：
   - 加入了对图表生成后实际存在的 `errorbar` 甚至子图 `line` 图元句柄数量的直接反射审计，生成 `final_figure_object_audit.txt`。

---

## 三、 论文交付物索引与核心代码结构

本工程目录下核心逻辑链与交付表单：

| 文件名称 | 说明与用途 |
| :--- | :--- |
| `PAPER_CODE_ALIGNMENT.md` | 记录算法特性与仿真条件的官方一致性断言声明 |
| `walkthrough.md` | 每一轮次的行动流、改动点与审查通过证明记录 |
| `results/paper_review/` | 所有经过审计、重整后的 `.csv` 数据报表与 `.txt` 审计存根 |
| `results/paper_figures/` | Fig1 - Fig5 的出版级源图 (PNG/PDF/FIG) 及独立追踪回放的 `_source.csv` |
| `src/analyze_paper2_paper.m` | 数据链路神经中枢：负责读取仿真 MAT，产出全量结构化 CSV 与一致性对比 |
| `src/generate_paper2_figures.m` | 绘图大本营：加载 CSV，绘制所有图表，执行句柄审计 |
| `lib/canonical_profile_id.m` | 全局严格的信道场景 `P1/P2/P3` 映射器 |

---

## 四、 下一步动作 (Next Steps)

* **最终手稿起草**：底层所有的科研活动、仿真调优与作图流程已**彻底冻结 (FINAL_3000MC_DATA_AUDITED)**。当前仓库状态 (`Round 10.3`) 提供了可以直接拿来放入 LaTeX 模板中的高质量 `.pdf` 矢量图。
* **数据与表格填报**：在起草论文的表格 (Tables) 和讨论时，可直接读取 `results/paper_review/` 目录下的一系列 CSV，其结果已经过严密的逻辑和置信度断言验证，绝无“口胡”成分。

> **WAITING FOR MANUSCRIPT SCIENTIFIC AUDIT.**
